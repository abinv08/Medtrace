// ─── API Service ──────────────────────────────────────────────────────────────
// Authentication is now handled directly by Firebase Auth + Firestore.
// This file is kept as a placeholder for future non-auth REST API calls
// (e.g. IoT sensor data endpoints, ML inference APIs).
//
// To re-enable HTTP calls, add the 'dio' package back to pubspec.yaml.

class ApiService {
  static const String baseUrl = 'http://localhost:5000/api';

  // Placeholder — add Dio instance here when needed for non-auth endpoints
}

